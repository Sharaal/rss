<?xml version="1.0" encoding="UTF-8"?>
<opml version="2.0">
	<body>
    {{#each feeds}}
      <outline text="{{title}}" htmlUrl="{{link}}" type="rss" xmlUrl="{{url}}"/>
    {{/each}}
	</body>
</opml>
